# MM-PTX Tools

## stack_ptx_generate_infos.py

This tool takes a `json` file such as in `type_descriptions/stack_ptx_descriptions.json` and makes a c or c++ header file that expresses these types. These type descriptions can be fed in to the Stack PTX system.
